# Repository validation

Validated on 2026-09-06: **6 tests passed and 1 optional predictive test was skipped** because scikit-learn was unavailable in the validation runtime.

The repository declares scikit-learn as a normal dependency, so the predictive test runs after standard setup. Live Tableau Server/Cloud publishing requires a Tableau account, permissions, and credentials; these are intentionally not included.
